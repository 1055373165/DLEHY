Translate Agent Mainline Handoff
Last Updated: 2026-04-05 00:58:41 +0800

1. 当前主线

当前主线已经不再是 runtime self-heal，也不是旧的 canonical-IR change_request 漂移线。

当前唯一有效主线是：

- 让 translate agent 在 `PDF 书籍 / EPUB 书籍 / PDF 论文` 上具备可证明的高保真翻译 readiness
- 用 benchmark 和 pilot 证据决定“能不能安全放量”
- 用 `slice-first` 而不是 blind full-document rollout 作为默认执行方式
- 在测试阶段把最大翻译测试单元固定为“章节”，避免整书测试浪费 token
- 把 Forge v2 的非法停机从 prompt 约束升级成 Hook 层强制规则，避免再次停下来等用户发继续

2. 当前进度

当前 measured truth 已经收口到：

- readiness current set: `overall = go`
- certified-lane pilots: 已完成首轮 `slice_target_reached`
- expansion wave 1: `overall = go`
- EPUB family wave 1: `overall = go`
- EPUB family wave 2: `overall = go`
- Forge v2 `Stop` hook guard：仓库入口和旧的 Codex-home `auto_commit.py` 入口都已收紧到显式 stop-state 白名单
- `epub-agentic-theories-001` 已 annotated + measured `go`，成为新的 `L1` widened evidence
- `epub-managing-memory-001` 也已 annotated + measured `go`，成为第二个 post-certification `L1` widened evidence
- `epub-managing-memory-001` 的章节级 pilot 已经真正对准 `OEBPS/ch05.html`
- 当前已把这个目标章完整收口到 `31/31` packets，`review_required = 0`
- 章节收口结果：`review.total_issue_count = 0`，review package 和 bilingual export 都已产出
- 章节 smoke 已修复成“续跑下一组 BUILT packets”，不会再重复扫前一刀窗口
- 默认翻译底层已从 DeepSeek 切换为 OpenRouter free Qwen，当前活跃模型串为 `qwen/qwen3.6-plus:free`
- 这次切换仍复用现有 `openai_compatible` 适配层，兼容地址为 `https://openrouter.ai/api/v1`
- 已完成一条极小在线探针并成功返回结构化 JSON，说明这不是只改了配置文件
- 旧的单一 USD 成本系数继续保持移除状态，避免在 free 路线下继续输出伪精确成本
- 新 provider 的第一条 live 证据已经开始落下：`epub-agentic-theories-001` Chapter 12 正在用 OpenRouter free Qwen 跑章节级 pilot
- 当前已连续完成两刀，共 `16` 个 packets，`review_required = 0`
- 当前测得的主要代价不是失败，而是高延迟：平均单包大约 `70.3s`，峰值约 `123s`

当前已认证 lane：

- `L1` `EPUB-reflowable-tech-book`
- `L2` `PDF-text-tech-book`
- `L3` `PDF-text-academic-paper`
- `L6` `High-artifact-density-paper`

关键依据：

- `/Users/smy/project/book-agent/artifacts/review/translate-agent-benchmark-execution-summary-current.json`
- `/Users/smy/project/book-agent/artifacts/review/translate-agent-benchmark-scorecard-current.json`
- `/Users/smy/project/book-agent/artifacts/review/translate-agent-lane-verdicts-current.json`
- `/Users/smy/project/book-agent/artifacts/review/translate-agent-pilot-summary-current.json`
- `/Users/smy/project/book-agent/artifacts/review/translate-agent-benchmark-expansion-wave1-execution-summary.json`

3. 现在最重要的变化

新的默认下一步不再是继续泛化地做 `L4` OCR-heavy 探索。

按用户最新指定，Forge v2 现在必须优先处理两份论文 PDF 样本：

1. `/Users/smy/Downloads/NIPS-2017-attention-is-all-you-need-Paper (1).pdf`
   - 作为单栏论文变体 probe
   - sample id: `pdf-attention-single-column-002`
   - 当前 measured 结果：`go`
2. `/Users/smy/Downloads/raft-atc14.pdf`
   - 作为双栏系统论文 probe
   - sample id: `pdf-raft-atc14-001`
   - 当前 measured 结果：`go`
   - 关键修复：第二页 title-page 论文识别进入 `academic_paper` lane，`9 Related work` 已恢复成正确 heading

4. Forge v2 接下来要做什么

P0

- 保留两份用户指定论文都作为 widened `L3` paper-variance success case
- 保留 `pdf-man-solved-market-zh-001` 和 `pdf-self-observation-zh-001` 作为 measured `L4` success case
- 保留 `epub-agentic-theories-001` 和 `epub-managing-memory-001` 作为 widened `L1` evidence
- 把 `epub-managing-memory-001` chapter 5 作为已完成的 chapter-complete pilot checkpoint 写回 truth
- 默认下一刀切到下一份已清 lane 的 EPUB live pilot，而不是继续消耗同一已完成章节
- 继续 `epub-agentic-theories-001` Chapter 12 的 bounded live slices，评估 OpenRouter free 路线是否只是“慢”，还是会在更长窗口里暴露新的 provider blocker
- 测试阶段的翻译验证上限是“章节”或等价章节切片，不做整书测试
- Stop legality 继续固定为显式 allowlist，活跃实现步骤不能再因为名字里含 `review` 就被当成合法停机边界

P1

- 在第一个 `L4` 结果之后，再决定是否继续 OCR-heavy lane 或切回额外 EPUB family 扩展
- 只有出现真实 measured blocker，才回到相关 hardening
- 只有在新样本 probe 成功后，才考虑为它们花新的整本文档翻译 token

5. 托管规则

- Forge v2 是这条主线的负责人
- 默认直接推进，不做非必要询问
- 一切从现有 `.forge/` 单一真相面继续，不另开 ledger
- Codex `Stop` hook 必须继续指向 Forge v2 stop guard；非法停机不再允许依赖聊天层自觉
- 默认下一刀：切到下一份 EPUB-family live pilot，并保留 `epub-managing-memory-001` chapter 5 作为已完成 checkpoint
